{
  "buildCommand": "npx expo export -p web --output-dir dist",
  "outputDirectory": "dist",
  "devCommand": "npx expo",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
